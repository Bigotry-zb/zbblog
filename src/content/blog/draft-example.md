---
title: 这是一篇草稿示例
description: '设置了 draft: true，生产构建时不会输出。'
pubDate: 2026-09-18
draft: true
tags: ['随笔']
---

`draft: true` 的文章：

- 本地 `npm run dev` 可以看到，方便预览
- `npm run build` 生产构建时会被排除，不会上线

写完后把 `draft` 改成 `false`（或删掉这行）再发布。
